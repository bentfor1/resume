<div>
    <p>

        Web page </p>
</div>
<div>
    <div class="web-languages">
        {{#each web.languages}}
        <div class="web-language">
            <h2>{{name}} ({{version}})</h2>
            <p>{{description}}</p>
            {{#if elements}}
            <h3>Elements</h3>
            <ul>
                {{#each elements}}
                <li>
                    <h4>{{name}}</h4>
                    <p>{{description}}</p>
                    {{#if attributes}}
                    <h5>Attributes</h5>
                    <ul>
                        {{#each attributes}}
                        <li>
                            <strong>{{name}}</strong> - {{description}}
                        </li>
                        {{/each}}
                    </ul>
                    {{/if}}
                </li>
                {{/each}}
            </ul>
            {{/if}}
            {{#if properties}}
            <h3>Properties</h3>
            <ul>
                {{#each properties}}
                <li>
                    <h4>{{name}}</h4>
                    <p>{{description}}</p>
                    {{#if values}}
                    <h5>Values</h5>
                    <ul>
                        {{#each values}}
                        <li>
                            <strong>{{value}}</strong> - {{description}}
                        </li>
                        {{/each}}
                    </ul>
                    {{/if}}
                </li>
                {{/each}}
            </ul>
            {{/if}}
            {{#if functions}}
            <h3>Functions</h3>
            <ul>
                {{#each functions}}
                <li>
                    <h4>{{name}}</h4>
                    <p>{{description}}</p>
                    {{#if parameters}}
                    <h5>Parameters</h5>
                    <ul>
                        {{#each parameters}}
                        <li>
                            <strong>{{name}}</strong> ({{type}}) - {{description}}
                        </li>
                        {{/each}}
                    </ul>
                    {{/if}}
                </li>
                {{/each}}
            </ul>
            {{/if}}
        </div>
        {{/each}}
        <div>
            <h1>My Title</h1>
            <p>My paragraph</p>
            <p>Another paragraph</p>
        </div>
    </div>