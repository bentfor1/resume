{{!--
Це код container файлу для summary сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div>

    {{#with main}}
    <main>
        <section>
            {{!-- {{> title data="Skills"}} --}}
            {{>title data="Skills" big=true}}
            <ul>
                {{#each scills}}
                <li>{{name}}
                    {{#if point includeZero=true}} ({{point}}) {{/if}}
                    {{#if isTop}} (TOP) {{/if}}
                    {{#unless point }} {{#if point includeZero=true}} (вивчаємо, але топчемся на нулю)
                    {{/if}}{{/unless}}
                    {{#unless point includeZero=true}} (вивчав, але забув) {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>
        <section>
            {{!-- {{> title data="My hobbies"}} --}}
            {{>title data="My hobbies" big=false}}
            <ul>
                {{#each hobbies}}
                <li>{{name}}
                    {{#if isMain}} (favorite) {{/if}}
                    {{!-- {{#if isTop}} (TOP) {{/if}}
                    {{#unless point }} {{#if point includeZero=true}} (вивчаємо, але топчемся на нулю)
                    {{/if}}{{/unless}}
                    {{#unless point includeZero=true}} (вивчав, але забув) {{/unless}} --}}
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
    {{/with}}


</div>

{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}